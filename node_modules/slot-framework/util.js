/**
 * Created by cecheveria on 8/21/2014.
 */

function startHeaderMsg(server, port, options) {
    //console.log("%s Server running at\n  => http://localhost:%s/", server, port);

    //console.log("\n  ---------------------");
    console.log("\n\n   %s Server on http://localhost:%s/\n\n", server.toUpperCase(), port);
    //console.log("  ---------------------\n");

    /**
     * TODO: Complete this login options

    console.log("DevMode:  %s", options.devMode);
    console.log("Caching:  %s", options.caching);
    console.log("Root:     %s", appFullPath());
     console.log("\n");
     //Compressing:
     //Minify:
     //Scaling Image:
     */

    console.log("CTRL + C to shutdown");
}
function appFullPath(uri) {
    /**
     * TODO:
     *  1.  Decide if we are going to return process.cwd() or "../..", or if we are going
     *      to evaluate a config parameter to detect if Server has been starte up using
     *      the 'slot start -d' command, or the server/development.js
     */
    return process.cwd() /*"../.."*/;
}

module.exports.appFullPath = appFullPath;
module.exports.startHeaderMsg= startHeaderMsg;